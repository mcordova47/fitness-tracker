<%=
  elmish_component(
    "Workouts/WorkOut",
    props: {
      currentPath: request.path,
      exerciseKinds: @exercise_kinds,
      muscleGroups: @muscle_groups,
      userId: @user_id
    },
    html_class: "h-100"
  )
%>
